{
  "name": "leetcraft",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=18.18.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "lint": "npm -ws run lint",
    "test": "npm -ws run test",
    "format": "npm -ws run format"
  }
}
